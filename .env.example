# PostgreSQL connection string (use Neon, Supabase, or any Postgres provider)
# For Neon: get this from your Neon dashboard → Connection Details
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Direct connection URL (required for Prisma migrations on Neon/PgBouncer)
DIRECT_URL="postgresql://user:password@host/dbname?sslmode=require"
